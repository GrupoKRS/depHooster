package utils

import "fmt"

/*Show ...
 * Operação de exibição, permite adicionar novos recursos a exibição na tela.
 * @author Rafael Gomides
 * @params {String} msg - Recebe a mensagem que será exibida
 */
func Show(msg string) {
	fmt.Println(msg)
}
